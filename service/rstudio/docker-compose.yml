version: "3"

services:
  rstudio:
    container_name: rstudio
    image: rocker/geospatial:4.4 #4.4
    ports:
      - "8787:8787"
    environment:
      PASSWORD: "CAL0813focas?"
      DISABLE_AUTH: true
    volumes:
      - rstudio_config:/home/rstudio/.config/rstudio
      - rstudio_workspace:/home/rstudio

    networks:
      pihole_net:
        ipv4_address: 192.168.2.11
    dns:
      - 192.168.2.2

networks:
  pihole_net:
    external: true

volumes:
  rstudio_config:
  rstudio_workspace: